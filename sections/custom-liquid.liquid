<div class="{% if section.settings.paddings %}section{% endif %} section--bordered">
	{{ section.settings.custom_liquid }}
</div>

{% schema %}
{
  "name": "custom_liquid.name",
  "tag": "section",
  "class": "section-dynamic section-borderable",
  "settings": [
    {
      "type": "checkbox",
      "id": "paddings",
      "label": "Add spacing",
	  "default": false
    },
    {
      "type": "liquid",
      "id": "custom_liquid",
      "label": "custom_liquid.settings.custom_liquid.label"
    }
  ],
  "presets": [
    {
      "name": "custom_liquid.presets.name"
    }
  ]
}
{% endschema %}
